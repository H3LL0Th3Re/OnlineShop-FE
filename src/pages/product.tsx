import ListProduct from '@/components/ListProduct';
import { Box } from '@chakra-ui/react';

const Product = () => {
  return (
    <Box>
      <ListProduct />
    </Box>
  );
};

export default Product;
