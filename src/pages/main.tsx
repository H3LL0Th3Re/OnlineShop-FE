import ListProduct from '@/components/ListProduct';
import { Box } from '@chakra-ui/react';

const Main = () => {
  return (
    <Box>
      {/* Main Content */}
      <ListProduct />
    </Box>
  );
};

export default Main;
